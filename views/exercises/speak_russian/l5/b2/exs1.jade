extends ../../../index.jade

block vars
	- var task = 'Listen, repeat, read:'
	- var items = 7

block columns
	.exercise_column
		.exercise_blocks_block
			- var texts = ['рубли́', 'до́ллары', 'е́вро', 'креди́тная ка́рта', 'креди́тка', 'сче́т', 'плати́ть за проду́кты']
			include ../../../types/text.jade
			- var path = '/images/' + course + '/exercises/' + ex_path + '/' + ('0' + select).slice(-2) + '.png'
			include ../../../types/image.jade

	.exercise_column
		.exercise_blocks_block
			- var path = '/audio/' + course + '/' + ex_path + '/' + ('0' + select).slice(-2) + '.m4a'
			include ../../../types/audio.jade
