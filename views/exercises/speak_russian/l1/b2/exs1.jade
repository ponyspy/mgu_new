extends ../../../index.jade

block vars
	- var task = 'Listen, repeat, and read words'
	- var items = 9

block columns
	.exercise_column
		.exercise_blocks_block
			- var texts = ['ме́неджер', 'студе́нтка', 'до́ктор', 'инжене́р', 'тури́стка', 'журнали́стка', 'бага́ж', 'Джо́н', 'Мари́я']
			include ../../../types/text.jade
			- var path = '/images/' + course + '/exercises/' + ex_path + '/' + ('0' + select).slice(-2) + '.png'
			include ../../../types/image.jade

	.exercise_column
		.exercise_blocks_block
			- var path = '/audio/' + course + '/' + ex_path + '/' + ('0' + select).slice(-2) + '.m4a'
			include ../../../types/audio.jade
