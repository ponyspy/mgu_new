extends ../../index.jade

block vars
	- var task = 'Выучите новые выражения'
	- var items = 15

block columns
	.exercise_column
		.exercise_blocks_block
			- var texts = texts = ['Cколько стоит завтрак?', 'Дайте мне счёт.', 'Вот ваша сдача.', 'Мы не принимаем карты.', 'Нет проблем!', 'О, как интересно!']
			include ../../types/text.jade
			- var path = '/audio/' + ex_path + '/' + ('0' + select).slice(-2) + '.m4a'
			include ../../types/audio.jade

	.exercise_column
		.exercise_blocks_block
			//- - var path = '/audio/' + ex_path + '/' + ('0' + select).slice(-2) + '.m4a'
			//- include ../../types/audio.jade
